#include <stdio.h>
#include <limits.h>

int main() {
	int i;
	// 여러개의 정수 값을 입력받으므로 배열을 이용한다
	// 입력 받는 정수의 개수가 정해져 있지 않다
	for (i = 0; i < ; i++) {
		printf("정수를 입력하세요 : ");
		scanf_s("%d", &j);
		a[i] = j;
	}
	//min = num
	return 0;
}